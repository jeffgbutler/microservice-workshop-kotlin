spring:
  application:
    name: movie-aggregator-service-rt

info:
  app:
    name: Movie Aggregator Service (RT)

management:
  endpoint:
    health:
      show-details: always
  endpoints:
    web:
      exposure:
        include: info,health

eureka:
  client:
    enabled: true
